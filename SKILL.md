---
name: theme-park-weather
description: 気象庁（JMA）の無料天気予報APIを使って、日本の主要テーマパーク所在地の「明日の天気」を取得し、天気の良さで優先順位を付けるためのスキル。「テーマパーク」「明日 天気」「どこ行くべき」「天気ランキング」「Discordに投稿」といった話題が出たら必ずこのスキルを使うこと。混雑度を使った優先度付けは対象外（別途データソースが必要）。
---

# テーマパーク天気優先度スキル

気象庁の天気予報API（無料・APIキー不要）を使い、主要テーマパーク6施設の所在地について
「明日」の天気を取得し、快晴・晴れ・曇り・雨の順で行き先の優先度を判定するためのスキル。

## できること / できないこと

- できる: 明日の天気（テキスト表現・天気コード・降水確率）の取得と、天気の良さによるランキング
- できない: 混雑度の取得（このスキルの対象外。将来 `references/congestion.md` を追加して拡張する想定）
- 注意: 気象庁の天気予報テキストには「快晴」という区分は基本的に出てこない（後述）。
  そのため「快晴 > 晴れ」の区別は天気コードだけでは厳密にはできず、近似的な扱いになる。

## 対象テーマパークとエリアコード

気象庁のエリアコードは階層構造（都道府県=offices → 地域=class10 → 市区町村=class15）になっている。
天気予報JSON自体は **offices（都道府県）コード** で取得し、その中の `areas` 配列から
該当する **class10（地域）コード** の項目を探して使う。

| テーマパーク | 所在地 | officesコード（API取得用） | 該当class10エリア名 / コード |
|---|---|---|---|
| 東京ディズニーリゾート | 千葉県浦安市 | `120000`（千葉県） | 北西部 / `120010` |
| ユニバーサル・スタジオ・ジャパン | 大阪府大阪市 | `270000`（大阪府） | 大阪市 / `270001` |
| ハウステンボス | 長崎県佐世保市 | `420000`（長崎県） | 北部 / `420020` |
| レゴランド・ジャパン | 愛知県名古屋市 | `230000`（愛知県） | 西部 / `230010` |
| 富士急ハイランド | 山梨県富士吉田市 | `190000`（山梨県） | 東部・富士五湖 / `190020` |
| 志摩スペイン村 | 三重県志摩市 | `240000`（三重県） | 南部 / `240020` |

※三重県の「南部」（`240020`）は伊勢志摩地域を含む区分名（気象庁の予報区分名は行政地名と一致しないことがある）。

このリストを増やしたい場合は、まず `https://www.jma.go.jp/bosai/common/const/area.json` を取得し、
`offices` の中から都道府県を探し、`class10s` の中から該当する `parent` が一致する地域名を探すこと。
`class10s` の名前が施設の所在地と直感的に一致しない場合があるので、`class15s`（市区町村レベル）の
`children` に対象市区町村のコードが含まれているかどうかで確認すると確実。

## 実行手順

### 1. 各施設のofficesコードで天気予報JSONを取得する

```
GET https://www.jma.go.jp/bosai/forecast/data/forecast/{officesコード}.json
```

例: 千葉県 → `https://www.jma.go.jp/bosai/forecast/data/forecast/120000.json`

同じofficesコードのテーマパークが複数ある場合でも、API呼び出しは1回で済む
（返ってくるJSON内に複数エリアの情報がまとめて入っている）。今回の6施設は全てofficesコードが異なるため、
**合計6回のリクエスト**になる。

### 2. レスポンスから「明日」のデータを取り出す

レスポンスはJSON配列で、`[0]` が短期予報（今日〜3日目くらい）、`[1]` が週間予報。
短期予報を使う。

```json
[
  {
    "publishingOffice": "...",
    "reportDatetime": "2026-07-12T17:00:00+09:00",
    "timeSeries": [
      {
        "timeDefines": ["2026-07-12T17:00:00+09:00", "2026-07-13T00:00:00+09:00", "2026-07-14T00:00:00+09:00"],
        "areas": [
          {
            "area": { "name": "北西部", "code": "120010" },
            "weatherCodes": ["101", "200", "100"],
            "weathers": ["晴れ　時々　曇り", "くもり　一時　雨", "晴れ"],
            ...
          }
        ]
      },
      {
        "timeDefines": [...],
        "areas": [
          { "area": {...}, "pops": ["10", "30", "20", "10", "0", "0", "0", "0"] }
        ]
      }
    ]
  }
]
```

手順:
1. `timeSeries[0].timeDefines` を見て、日付が「明日」に一致するインデックス `i` を探す
   （`reportDatetime` の日付と比較し、+1日のものを選ぶ。通常は今日=0, 明日=1, 明後日=2）
2. `timeSeries[0].areas` から `area.code` が対象のclass10コード（上表参照）と一致する要素を探す
3. その要素の `weatherCodes[i]` と `weathers[i]` を「明日の天気」として採用する
4. 降水確率（`timeSeries[1].pops`）も同じエリアコードで拾えると、同ランク内の並び替えに使える
   （3時間ごとの値が複数入っているので、日中帯の値を2〜3個平均するとよい）

補足: `weathers`配列には「晴れ　時々　曇り」のような**日本語テキストがそのまま**入っているので、
このテキストをそのまま使えば天気コードのテキスト変換表は不要（Discord投稿にもそのまま使える）。
天気コードの数値（`weatherCodes`）はランク分け・絵文字選択の判定にだけ使う。

### 3. 天気コードを優先度ランクに変換する

気象庁の天気コード（`weatherCode`）は3桁で、**先頭の数字がおおまかな天気カテゴリ**を表す。

| 先頭 | カテゴリ | 優先度ランク |
|---|---|---|
| 1xx | 晴れ系（100=晴れ、101=晴時々曇 等） | ランク高 |
| 2xx | 曇り系（200=くもり 等） | ランク中 |
| 3xx | 雨系（300=雨 等） | ランク低 |
| 4xx | 雪系 | 今回のテーマパーク立地では基本発生しない想定。発生したら雨と同等かそれ以下として扱う |

**「快晴」について**: 気象庁の予報テキスト・天気コードには「快晴」という独立区分が原則存在しない
（快晴は主に実況値で使われる区分で、予報の `weathers` 文字列にはまず出てこない）。
そのため「快晴 > 晴れ」を厳密に区別することはできない。運用上は以下のどちらかを選ぶ:

- **簡易対応（デフォルト）**: 1xx系はすべて同ランク「晴れ」として扱い、同ランク内は降水確率（pops）が低い順に並べる
- **文字列一致で疑似対応**: `weathers` の文字列に「快晴」という語が実際に含まれていた場合のみ最上位ランクとして扱う
  （出現頻度は低いので、これに依存しすぎない）

このスキルを使うときは、ユーザーから特に指定がなければ「簡易対応」をデフォルトとして進める。

### 4. 総合ランキングを作る

各テーマパークについて (a) 天気ランク（晴れ>曇り>雨>雪）→ (b) 同ランク内は降水確率が低い順、
の2段階でソートし、上から順に「明日行くべき」順位とする。

出力するときは、テーマパーク名・所在地・明日の天気テキスト・降水確率・順位を含めること。
Discordへの投稿は本スキルの範囲外（投稿はDiscord MCPコネクタ側のツールを使う）。

## 出力フォーマット例（Discord投稿用）

```
🎢 明日のテーマパーク天気ランキング（2026-07-13）

1位 🌤 東京ディズニーリゾート（千葉・浦安）— 晴れ時々曇り（降水確率10%）
2位 ☀️ ユニバーサル・スタジオ・ジャパン（大阪）— 晴れ（降水確率20%）
3位 ☁️ レゴランド・ジャパン（愛知・名古屋）— くもり（降水確率30%）
4位 ☁️ 志摩スペイン村（三重）— くもり時々雨（降水確率40%）
5位 🌧 富士急ハイランド（山梨）— 雨（降水確率70%）
6位 🌧 ハウステンボス（長崎・佐世保）— 雨（降水確率80%）

※混雑度は未考慮です
```

## 拡張のヒント

- 混雑度を追加する場合、テーマパーク公式の混雑予想ページや外部の混雑予測サービスをスクレイピング/APIで取得し、
  `references/congestion.md` のようなファイルを追加して、このSKILL.mdから参照するとよい
  （progressive disclosureの考え方に沿って、混雑ロジックは別ファイルに分離するのが望ましい）
- テーマパークを追加したい場合は「対象テーマパークとエリアコード」の表に行を足すだけでよい
